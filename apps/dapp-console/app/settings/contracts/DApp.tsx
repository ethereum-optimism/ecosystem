'use client'

import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from '@eth-optimism/ui-components'

export const App = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>New App</CardTitle>
      </CardHeader>
      <CardContent>TODO</CardContent>
    </Card>
  )
}
